<div class="card" style="width: 18rem;">
 <% if user.avatar_attachment.present? %>
      <%= image_tag( user.avatar ,class: "card-img-top") %>
    <%end%>
    <div class="card-body">

    <h5 class="card-title"><%=user.name%></h5>
<h6 class="card-title"><%=user.locations.last.longitude%> , <%=user.locations.last.latitude%></h6>

     <a href="#" class="btn btn-primary">Flag</a>
  </div>
</div>
