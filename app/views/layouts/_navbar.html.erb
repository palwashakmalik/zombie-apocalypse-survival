<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Zombie Apocalypse Survival</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link"  %>
        </li>
        <li class="nav-item">
          <%= link_to "Locations", locations_path, class: "nav-link"  %>
        </li>
        <% if  !current_user.infected?%>
          <li class="nav-item">
            <%= link_to "Items", user_items_path(current_user), class: "nav-link"  %>
          </li>
          <li class="nav-item">
            <%= link_to "Trade History", user_trades_path(current_user), class: "nav-link"  %>
          </li>
          <li class="nav-item">
            <%= link_to "Settings", edit_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
        <li class="nav-item">
          <% if current_user.avatar_attachment.present? %>
            <%= image_tag(current_user.avatar ,class: "rounded-circle avatar") %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
